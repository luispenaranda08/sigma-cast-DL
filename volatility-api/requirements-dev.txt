# ── Development/test-only dependencies ────────────────────────────────────
-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2          # required by fastapi.testclient
ruff==0.6.9            # lint + format, zero-config
